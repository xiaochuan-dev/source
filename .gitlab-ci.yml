stages:          # List of stages for jobs and their order of execution
  - publish

publish-job:
  stage: publish
  image: node
  only: [tags]
  script:
    - export YARN_AUTH_TOKEN=${NPM_TOKEN}
    - yarn publish --non-interactive