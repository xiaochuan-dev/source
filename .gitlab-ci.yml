stages:          # List of stages for jobs and their order of execution
  - publish

publish-job:
  stage: publish
  image: node
  only: [tags, web]
  script:
    - YARN_AUTH_TOKEN=${NPM_TOKEN} yarn publish --non-interactive